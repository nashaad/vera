# Getting started

Vera currently runs from a source checkout.

Install dependencies:

```sh
bun install
```

Choose one provider.

## Codex subscription

No API key or Codex CLI is required.

Sign in:

```sh
bun run clients/cli/main.ts login openai-codex
```

Complete sign-in in the browser. Vera stores credentials in
`~/.vera/auth.json` and refreshes them automatically. Do not edit this file.

Create `~/.vera/config.json`:

```json
{
  "schema_version": 1,
  "provider": "openai-codex",
  "model": "gpt-5.6-sol",
  "reasoning_effort": "medium"
}
```

`reasoning_effort` may be `off`, `low`, `medium`, `high`, or `max`. If omitted,
the model uses its default. Use `off` to disable reasoning.

For other OpenRouter models, Vera reads the model's supported levels. If the
mapped level differs, the status line shows both values, such as
`thinking max → xhigh (inferred)`. Vera does not infer `off`.

To use a backup model from the same provider, add:

```json
{
  "fallback": {
    "model": "your-backup-model",
    "after_failures": 3
  }
}
```

`after_failures` may be 1, 2, or 3. Only consecutive rate-limit or server
failures select the backup. Once selected, it remains active through the rest
of that turn. OpenAI Codex fallback currently requires `reasoning_effort` to be
omitted; OpenRouter resolves the configured effort against each model.

## OpenRouter

Set `OPENROUTER_API_KEY`, then create `~/.vera/config.json`:

```json
{
  "schema_version": 1,
  "provider": "openrouter",
  "model": "anthropic/claude-sonnet-5",
  "reasoning_effort": "medium"
}
```

## Run

```sh
bun run tui
```
