# Optional adversarial review adapter

This example extension adapts the runnable application in
`examples/sdk-reviewer/` into extension command and tool surfaces. It registers
those surfaces only when a user explicitly configures this example extension.
Vera does not load it by default.
